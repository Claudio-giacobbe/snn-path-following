# Dataset SNN

The full training dataset is not included in the repository.

Generate it with:

```bash
python src/collect_snn_dataset.py
```

The script creates:

```text
dataset_snn/
├── images/
└── labels.csv
```

The default target is 3000 samples.
